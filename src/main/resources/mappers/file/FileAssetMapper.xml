<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fss.backend.file.FileAssetMapper">
    <insert id="create">
        INSERT INTO files(id, path, status, created_by, updated_by)
        VALUES(#{id}, #{path}, #{status}, #{createdBy}, #{createdBy})
    </insert>

    <select id="findById" resultType="com.fss.backend.file.FileAsset">
        SELECT id, path, status, created_at
        FROM files
        WHERE id = #{id}
    </select>

    <select id="findByPath" resultType="com.fss.backend.file.FileAsset">
        SELECT id, path, status, created_at
        FROM files
        WHERE path = #{path}
    </select>

    <update id="updateStatus">
        UPDATE files
        SET status = #{status}, updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <select id="countActiveReferences" resultType="int">
        SELECT
            COALESCE((SELECT COUNT(*)
                      FROM product_images pi
                      JOIN products p ON p.id = pi.product_id
                      WHERE pi.file_id = #{id}
                        AND p.deleted_at IS NULL), 0)
            + COALESCE((SELECT COUNT(*)
                        FROM product_variants pv
                        JOIN products p ON p.id = pv.product_id
                        WHERE pv.image_file_id = #{id}
                          AND pv.deleted_at IS NULL
                          AND p.deleted_at IS NULL), 0)
            + COALESCE((SELECT COUNT(*)
                        FROM brands b
                        WHERE b.file_id = #{id}
                          AND b.deleted_at IS NULL), 0)
            + COALESCE((SELECT COUNT(*)
                        FROM brands b
                        WHERE b.file_size_id = #{id}
                          AND b.deleted_at IS NULL), 0)
            + COALESCE((SELECT COUNT(*)
                        FROM landing_banners lb
                        WHERE lb.file_id = #{id}
                          AND lb.deleted_at IS NULL), 0)
            + COALESCE((SELECT COUNT(*)
                        FROM collections c
                        WHERE c.file_id = #{id}
                          AND c.deleted_at IS NULL), 0)
            + COALESCE((SELECT COUNT(*)
                        FROM blog_posts bp
                        WHERE bp.cover_file_id = #{id}
                          AND bp.deleted_at IS NULL), 0)
            + CASE
                WHEN #{path} IS NULL OR #{path} = '' THEN 0
                ELSE COALESCE((SELECT COUNT(*)
                               FROM products p
                               WHERE p.deleted_at IS NULL
                                 AND p.description_html IS NOT NULL
                                 AND POSITION(CONCAT('/files/', #{path}) IN p.description_html) &gt; 0), 0)
                   + COALESCE((SELECT COUNT(*)
                               FROM collections c
                               WHERE c.deleted_at IS NULL
                                 AND c.description_html IS NOT NULL
                                 AND POSITION(CONCAT('/files/', #{path}) IN c.description_html) &gt; 0), 0)
                   + COALESCE((SELECT COUNT(*)
                               FROM blog_posts bp
                               WHERE bp.deleted_at IS NULL
                                 AND bp.content_html IS NOT NULL
                                 AND POSITION(CONCAT('/files/', #{path}) IN bp.content_html) &gt; 0), 0)
              END
    </select>

    <select id="listInactiveCreatedBefore" resultType="com.fss.backend.file.FileAsset">
        SELECT id, path, status, created_at
        FROM files
        WHERE status = 'INACTIVE'
          AND created_at &lt; #{cutoff}
    </select>

    <delete id="deleteById">
        DELETE FROM files
        WHERE id = #{id}
    </delete>
</mapper>
